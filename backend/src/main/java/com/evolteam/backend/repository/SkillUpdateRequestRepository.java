package com.evolteam.backend.repository;

import com.evolteam.backend.entity.SkillUpdateRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SkillUpdateRequestRepository extends JpaRepository<SkillUpdateRequest, Long> {
}